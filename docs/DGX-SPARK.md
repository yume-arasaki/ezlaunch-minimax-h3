# DGX Spark (GB10) — MiniMax-H3 on a single unified 128GB machine

Recipe + attribution: Hamster 2026-08-25 + Beaver 2026-08-25 field receipts.
Everything below is sourced, nothing invented.

## Primary recipe: drowzeys keys-SM121 ... Sol-Engine (THE stack)

**drowzeys/keys-SM121-Optimized-MiniMax-H3-Nvidia-Sol-Engine-Kijai-SolAttn_Triton-Single-DGX-Spark**
is the primary single-Spark recipe. It is the only stack that both (a) fixes
H3-on-SM121 correctness and (b) measures a real speedup. Apache-2.0.

- Fixes the silent `flex_attention` miscompile on SM121 (cos-sim 0.92–0.97 vs
  SDPA — no error, just garbage that looks like a broken checkpoint).
- **Measured on idle GB10, seed-matched: 312.7s → 202.5s** for a 5s @ 864×480
  clip with native stereo audio (**1.54× E2E, 1.72×/step**).
- One-shot install (`./install.sh`: pinned ComfyUI v0.30.1 + 41 GB weights +
  all four accelerations) and an agent-executable `RECIPE.md`.

```bash
git clone https://github.com/drowzeys/keys-SM121-Optimized-MiniMax-H3-Nvidia-Sol-Engine-Kijai-SolAttn_Triton-Single-DGX-Spark.git
cd keys-SM121-*
./install.sh                    # ComfyUI v0.30.1 + 41GB weights + accelerations
~/comfy/h3-comfy-launch.sh      # launcher: reserve-vram 8, CUDA 13 on PATH
# drag ~/comfy/h3-fullstack.json onto the canvas, hit Run
```

What's in the stack & why each part helps (all numbers drowzeys, idle/seed-matched):

| Optimization | Origin | Measured |
|---|---|---|
| **Sol-Attn** (Triton, INT8 QK + TMA) | kijai / KingGore, patched for 12.1 here | 1.32×/step |
| **FirstBlockCache** | NVIDIA `cache_line.py` → ported to ComfyUI single-GPU | 1.48×/step |
| **Batched VAE tile decode** | NVIDIA `vae_shard.py` → ported | 1.66× on decode |
| Full stack | — | **1.54× E2E, 1.72×/step** |

Also answers the multi-node question so you don't spend the week: Spark fabric
is 5.52 GB/s vs the 503 GB/s NVLink NVIDIA's 3.97× assumed → **CP=2 is slower**
than one tuned node. Scaling = throughput, not latency.

## Measured receipts (ours + drowzeys)

| Load | Time | Config |
|---|---|---|
| 5s @ 864×480, full stack | **202.5s** (M, drowzeys bench) | 1.54× vs stock 312.7s |
| **10s @ 720p, 20-step** | **~20 min (M, ours)** | drowzeys stack, field-verified |
| 15s @ 384×672 AV (362f) | ~15 min class (M, chishiki37) | base AV correctness path |
| **15s / 362f (model max)** | **~32 min, leaves 10–13 GB free** (M, drowzeys) | the real single-Spark ceiling |

**Ablation (drowzeys, idle):** stock 14.40 s/step → +Sol-Attn 10.88 → +FBC 9.72
(215.7s E2E) → full stack 8.39 s/step (202.5s E2E).

## Reference: chishiki37 (AV-correctness base)

chishiki37/minimax-h3-av-comfyui-recipe is the reference for a *correct* H3
audiovisual run on GB10 (video + audio both generated by H3, not TTS), and is
where our AV node-graph discipline comes from. Use it as the base graph if you
run the un-accelerated path; **drowzeys is the faster default.**

https://github.com/chishiki37/minimax-h3-av-comfyui-recipe

## Detection reality (important)

`nvidia-smi --query-gpu=name,compute_cap,memory.total` on GB10 returns:

```
NVIDIA GB10, 12.1, 128 GB        # compute_cap 12.1 = SM121 (≠ SM100/89!)
```

- **Memory shows "Not Supported"** — unified memory, no dedicated VRAM.
  Use `free` / `top` / DGX Dashboard (https://localhost:11000).
- SM121 needs **CUDA 13 builds** (vLLM requires `cu130-nightly`). Many kernels
  don't support SM121 yet.
- EZlaunch detection name-locks on `"GB10"` and skips the VRAM path entirely.
- Driver ≥ 580 (CUDA 13).

## Weights (pinned revisions)

- TE: `sakamakismile/Qwen3-VL-32B-Heretic-MiniMax-H3-NVFP4` rev `2814607c…`
  → `models/text_encoders/MiniMax-H3/qwen3vl_32b_heretic_minimax_h3_nvfp4.safetensors`
- Diffusion + VAEs: `Comfy-Org/MiniMax-H3` rev `dcc9da70…`
  - `diffusion_models/minimax_h3_fl2va_pruned_int8_convrot.safetensors` (~20 GB)
  - `vae/minimax_h3_video_vae_fp16.safetensors`
  - `vae/minimax_h3_audio_vae_fp32.safetensors`

Placement: `models/{text_encoders,diffusion_models,vae}/MiniMax-H3/`.

## Node graph (the correct AV path)

1. `UNETLoader` → pruned int8 base, `weight_dtype: default`
2. `MiniMaxH3SigmaShift` → `shift_video 12.0`, `shift_audio 3.0`
3. `CLIPLoader` → Heretic NVFP4 TE, `type: minimax`
4. `VAELoader` (video) + `VAELoader` (audio)
5. **`MiniMaxH3ReferenceToVideo`** (NOT ImageToVideo) — width 384, height 672,
   `length 362` (~15.08s @ 24fps), `ref_image_size: match`
6. `ConditioningZeroOut` for negative conditioning
7. `KSampler` → `res_multistep`, `simple`, **cfg 1.0, steps 20** (10 was patchier)
8. `VAEDecode` (video) + `VAEDecodeAudio` (audio)
9. `CreateVideo` → fps 24, bit_depth 8
10. `SaveVideo` → mp4, h264

Verified output: 384x672 / 24fps / 362 frames / ~15.08s, aac audio, ~1.0MB.

## Recursion: these apply to ALL tiers, confirmed again on Spark

- Frame count **17n+5** (362 = 15.08s ✓) — see `scripts/` for the validator hook
- `--disable-pinned-memory --fp16-intermediates expandable_segments` baked in
- `memory_usage_factor` 0.114 → 1.0 patch (EZlaunch does this on install/launch)
- res_multistep via BasicGuider; **KSamplerSelect alone = IndexError**
- `VAEDecodeTiled` is a no-op / fails on nested H3 latents — use plain `VAEDecode`

## Non-negotiables on Spark

| Rule | Why / source |
|---|---|
| **Never** `--use-sage-attention` with H3 | pure noise on GB10 (drowzeys + ComfyUI#15263) |
| `flex_attention` on sm_121 = **silently wrong** (cos sim 0.92–0.97 vs SDPA) | drowzeys — forces Triton kernel on 12.1; numerically verify every kernel |
| H3 blocks mutate input in place → need 2× `.clone()` | drowzeys (else FirstBlockCache diff=0 → checkerboard) |
| NVIDIA cache threshold assumes 50 steps → port adds start_step / end_dense / max_consecutive_skips | drowzeys |
| SageAttention pinned **pre-3.0** if Sage used at all | 3.x = mosaic artifacts on GB10 (bjarkebolding) |
| `swap off`, `swappiness 10`, driver persistence, `/dev/shm ≥ 16g` | spark-comfyui |
| Never benchmark a busy GB10 | reads like thermal throttling (10.88 → 20.77 s/step) |
| Co-tenancy: start LLM first, H3 after; restarting LLM → stop H3 first | our hardware compile |

## Resources

- Disk: chishiki37 set ~40 GB; drowzeys install ~41-45 GB; luqidaxia full ~171 GB.
  **≥200 GB recommended.**
- RAM: ≥96 GB free before launch recommended (110 GB ideal). Physical 128 GB.
- 5s render leaves ~26 GB free; 15s (362f) leaves 10–13 GB (drowzeys measured).

## Serve

```bash
nohup <venv>/bin/python main.py --listen 0.0.0.0 --port 8188 --lowvram \
  > ~/comfy/logs/comfy-8188.log 2>&1 &
curl -fsS http://127.0.0.1:8188/system_stats
```

## Attribution

| Recipe / dependency | URL |
|---|---|
| **drowzeys keys-SM121 Sol-Engine (primary)** | github.com/drowzeys/keys-SM121-Optimized-MiniMax-H3-Nvidia-Sol-Engine-Kijai-SolAttn_Triton-Single-DGX-Spark |
| — Sol-Attn Triton + INT8/TMA | kijai/ComfyUI-SolAttn_triton (no license — cloned, not vendored) |
| — ComfyUI_sol-attn_Blackwell (pre-patched, Apache-2.0) | KingGore/ComfyUI_sol-attn_Blackwell |
| — ported from NVIDIA Sol-Engine (Apache-2.0) | NVlabs/Sana sol-engine · paper arXiv:2607.24027 |
| chishiki37 (AV-correctness reference) | github.com/chishiki37/minimax-h3-av-comfyui-recipe |
| luqidaxia one-click | github.com/luqidaxia/dgxspark_comfyui_minimax_h3 |
| riverar / hoomns vLLM-Omni serving lane | github.com/riverar/MiniMax-H3-DGX-Spark · github.com/hoomns/MiniMax-H3-DGX-Spark |
| newjordan h3-spark | github.com/newjordan/h3-spark |
| bjarkebolding spark-comfyui | github.com/bjarkebolding/spark-comfyui + NVIDIA forum thread |
| Full attribution | `workspace/drafts/ezlaunch-h3-spark-beaver-attribution-2026-08-25.md` |
